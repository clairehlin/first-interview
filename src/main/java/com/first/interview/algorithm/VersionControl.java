package com.first.interview.algorithm;

public class VersionControl {
    boolean isBadVersion (int version) {
        if (version <= 3) {
            return false;
        } else {
            return true;
        }
    }
}
